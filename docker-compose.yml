

version: "3.9"

services:
  db:
    image: postgres:15
    container_name: inventary_db
    # environment:
    #   POSTGRES_USER: postgres
    #   POSTGRES_PASSWORD: password
    #   POSTGRES_DB: inventary_dbdummy
    env_file: .env.docker
    ports:
      - "5432:5432"
    # healthcheck:
    #   test: ["CMD-SHELL", "pg_isready -U postgres"]
    #   interval: 5s
    #   timeout: 5s
    #   retries: 5
    volumes:
      - postgres_data:/var/lib/postgresql/data

  app:
    build: .
    container_name: inventary_data
    env_file: .env.docker
    depends_on:
      - db
      # db:
      #   condition: service_healthy

    ports:
      - "8000:8000"

volumes:
  postgres_data:








    



    

